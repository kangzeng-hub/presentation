# Backward-compatible entry point. New installs should use the repository-level
# requirements files so runtime, workbench, development, and PPT dependencies
# remain explicitly separated.
-r ../requirements.txt
