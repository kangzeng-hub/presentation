"""Unified Presentation workspace domain model.

This layer owns cross-module references and validation. Existing image,
generation, QA, and calibration models remain the execution schemas.
"""
from .models import *

